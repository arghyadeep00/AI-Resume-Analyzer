services:
  frontend:
    #build:
      #context: ./Frontend
      #args:
     #   - NEXT_PUBLIC_API_URL=${NEXT_PUBLIC_API_URL:-http://localhost:5000}
    build: ./Frontend
    ports:
      - "3000:3000"
    env_file:
      - ./Frontend/.env
  
  backend:
    build: ./Backend
    ports:
      - "5000:5000"
    env_file:
      - ./Backend/.env

